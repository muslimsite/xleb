/* =====================================================================
   НАСТРОЙКА TELEGRAM  —  заполните 2 значения ниже
   =====================================================================

   Заказы с сайта будут приходить в ваш Telegram-чат (группу).

   ── ШАГ 1. Создать бота и получить ТОКЕН ────────────────────────────
   1) Откройте в Telegram бота  @BotFather
   2) Отправьте команду  /newbot
   3) Придумайте имя и username бота (username должен заканчиваться на "bot")
   4) BotFather пришлёт строку вида:
        123456789:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
      Это и есть TOKEN — вставьте его ниже в BOT_TOKEN.

   ── ШАГ 2. Создать группу и получить CHAT_ID ───────────────────────
   1) Создайте в Telegram группу, напр. «Заказы — Тёплый хлеб»
   2) Добавьте в группу своего бота (по его username) и пекаря
   3) Напишите в группе любое сообщение (например «старт»)
   4) Откройте в браузере ссылку (подставьте свой токен вместо <TOKEN>):
        https://api.telegram.org/bot<TOKEN>/getUpdates
   5) Найдите в ответе блок  "chat":{"id":-1001234567890, ...}
      Число  id  (для группы оно ОТРИЦАТЕЛЬНОЕ) — вставьте в CHAT_ID ниже.

   Готово. Сохраните файл, закоммитьте, заказы пойдут в группу.

   ⚠️ Токен виден в коде сайта. Это нормально для небольшого проекта.
      Если токен скомпрометируют — перевыпустите его у @BotFather (/revoke).
   ===================================================================== */

window.SHOP_CONFIG = {
  // 👇 ВСТАВЬТЕ СЮДА ТОКЕН БОТА от @BotFather (между кавычками).
  //    Это та строка вида 8944753210:AAE..., которую вы подставляли в ссылку getUpdates.
  BOT_TOKEN: "8944753210:AAGhT_qjFvvosxYHQgInT-tbeOfUFplm_gI",

  // id группы «Заказы — тёплый хлеб» (уже заполнен):
  CHAT_ID: "-5399062515"
};
